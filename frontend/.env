# Demo mode: serves static fixtures instead of calling the 2023 backend.
# To run against the real stack (PostgreSQL + Cassandra + Express), create
# a .env.local with REACT_APP_DEMO=false
REACT_APP_DEMO=true
